# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:Shibboleth
#
# ~~~~

*       /login                  shib.Shibboleth.login
*       /authenticate           shib.Shibboleth.authenticate
*       /logout                 shib.Shibboleth.logout